# can store elements of different types
# key must be immutable so can't use list as key, but can use tuple
bike = {"make": "Honda", "engine_size": 250}

print(bike["engine_size"])
print(bike["make"])
